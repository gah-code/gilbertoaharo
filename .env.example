# ============================================================================
# CONTENTFUL API CONFIGURATION
# ============================================================================
# Get these values from your Contentful Space Settings > API Keys
# https://app.contentful.com/spaces/{SPACE_ID}/api/keys

VITE_CONTENTFUL_SPACE_ID=
# The environment within your space (usually 'master' for production)
VITE_CONTENTFUL_ENVIRONMENT=master
# Delivery API access token (read-only, safe to commit to public repos)
VITE_CONTENTFUL_DELIVERY_TOKEN=

# ============================================================================
# SITE CONFIGURATION
# ============================================================================
VITE_SITE_URL=http://localhost:5173
# URL prefix for article routes (e.g., /articles/:slug)
VITE_ARTICLE_ROUTE_PREFIX=/articles

# ============================================================================
# BUILD AND CONTENT MODE
# ============================================================================
# Controls whether we allow preview toggles at runtime.
# Values: 'prod' (production, no toggles) | 'preview' (dev/preview, allows UI toggles)
# For prod builds, keep as 'prod'.
VITE_BUILD_TARGET=prod
# Content source integration. Currently: 'contentful' (required)
VITE_CONTENT_SOURCE=contentful
